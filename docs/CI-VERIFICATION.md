# CI Verification

GiwaBuildProof uses GitHub Actions to verify the Hardhat project.

The CI workflow compiles the project successfully before a Mergit bounty pull request is merged.

This provides a reproducible build check for the repository.
